# Security Policy

## Supported Versions

Only the latest version is supported.

You can check for updates using these methods:

1. While running, in settings, there is a button labelled "Check For Updates".
2. You can run ``git fetch`` to manually check for updates.

To update, ensure everything is closed, then run ``git pull``.

## Reporting a Vulnerability

Report vulnerabilities in the security tab.
